{
  "schema_version": 1,
  "description": "Repository shape contract consumed by tests/skill-eval-fixtures.test.mjs; not JSON Schema. Workflow cases require fixture files. Description-only routing uses a standalone prompt or a labeled prompts array. Assertions are expected behavior, not observed results.",
  "catalog_required_keys": [
    "schema_version",
    "purpose",
    "execution",
    "cases"
  ],
  "required_keys": [
    "id",
    "prompt",
    "assertions"
  ],
  "types": {
    "id": "string",
    "prompt": "string",
    "kind": "string",
    "assertions": "array",
    "files": "object",
    "prompts": "array",
    "fixture_note": "string"
  },
  "constraints": {
    "assertions_items_type": "string",
    "files_values_type": "string",
    "kinds": [
      "workflow",
      "description-only-routing"
    ]
  }
}
